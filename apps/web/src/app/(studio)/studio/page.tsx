/**
 * This is a studio landing page. From Studio open applications.
 * @returns A functional component named Page is being returned, which renders a div element with the
 * text "Studio" inside.
 */
export default function Page() {
  return (
    <div>
      {/* Shows studio page as an application dashboard with metrics */}
    </div>
  );
}
